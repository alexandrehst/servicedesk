---
scope: Service desk interno MCP-first (nucleo API+MCP)
purpose: build-substrate
altitude: initiative
updated: 2026-08-08T12:55
---

- (event) Spine iniciada do PRD final (27 FRs, superficie MCP como contrato). Greenfield. 1 pessoa + IA, stack boring, schema minimo. Escala pequena (100 users, 8 agentes, ~200-400 chamados/mes).
- (version) MCP TypeScript SDK v2 (estavel, spec 2026-07-28, protocolo stateless request/response); pacotes focados @modelcontextprotocol/server e /client + adapters Node/Express/Hono/Fastify. TS e a linguagem 'nativa' do MCP. FastMCP-TS (punkpeye) ainda nao suporta a spec 2026-07-28 stateless.
- (decision) [ADOPTED user] Paradigma: Hexagonal (Ports & Adapters). Nucleo de dominio sem deps externas; adapters de entrada MCP e API HTTP batem no mesmo nucleo; adapters de saida persistencia e e-mail. Codifica o invariante 'UI e MCP consomem a mesma camada de dominio'.
- (decision) [ADOPTED user] Stack: TypeScript ponta-a-ponta (dominio, API, servidor MCP, futura UI web). Persistencia PostgreSQL. Validacao/contratos com Zod. SDK MCP oficial v2. Motivo: TS nativo do MCP, uma linguagem so, stack boring para 1 pessoa+IA.
- (decision) [ADOPTED user] Entregavel: ARCHITECTURE-SPINE.md + deck visual HTML (hexagono, fluxo de tool MCP, ERD).
- (version) Verificado ago/2026: Node.js 24 LTS ativo (22 manutencao); PostgreSQL 18.4; Hono 4.13.x; Zod 4.4.x; Drizzle ORM 0.45.x (v1 em beta); MCP TS SDK v2 (@modelcontextprotocol/server, spec 2026-07-28).
- (decision) Invariantes definidos: dependencia so para dentro (hex); dominio unico ponto de mutacao; auditoria transacional com autor+origem; Numero gerado por sequence do banco (imutavel); Status maquina de estados fechada validada no dominio; contratos MCP/API de uma unica fonte Zod; acoes irreversiveis exigem confirmacao no dominio; autorizacao (papel+posse) no dominio; identidade+origem propagadas ao audit.
- (decision) AD-10 adicionado apos passada adversarial: concorrencia otimista (version/updated_at esperado) aplicada no command handler, para MCP e API tratarem edicao concorrente igual. Fecha buraco do FR-7 NFR.
- (event) spine finalized
